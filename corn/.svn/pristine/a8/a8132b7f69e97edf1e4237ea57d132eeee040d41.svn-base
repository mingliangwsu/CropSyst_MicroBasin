#include <corn/format/html/img.h>

namespace HTML
{
void Img::options()
{
   Element::options();
   Attribute_options::write(document);
   Data_options::write(document);
   Dimension_options::write(document);
   Align_option::write(document);
   src.write(document);
   alt.write(document);

   /*NYI
   CORN_string    longdesc;
   uint8          border;
   uint16         hspace;
   uint16         vspace;
   bool           ismap
   CORN_string    usemap;
   bool           controls ?
   bool           dynsrc   ?
   CORN_string    longdesc ?
   ??             loop;
   CORN_string    lowsrc; ???
   CORN_string    name;
   CORN_string    start;???
   CORN_string    VRML; ???
   bool           suppress;
   */
};
};
