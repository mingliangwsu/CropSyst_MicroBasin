#ifdef OBSOLETE
shouldn't need to load the stations we can read from the indexed database

bool load_Canadian_Climatological_stations(CORN::Association_list &locations)
{  bool loaded = true;

for each record in station

   return loaded;
};
#endif
//______________________________________________________________________________
   Canadian_climatological_station_record station_rec;
   //        Now output the location record
   {  VV_File loc_file(loc_filename.c_str());
      loc_file.set(loc_rec);
   };
   // For some reason the file was being set to read only, make sure it is not set to read only
   chmod(loc_filename.c_str(),S_IREAD | S_IWRITE);
//______________________________________________________________________________

